services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:5192/api}
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
